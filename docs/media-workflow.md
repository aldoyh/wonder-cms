# Media workflow

Place source media in `media/`. Run:

```bash
bash scripts/collect-media.sh
```

The script copies publishable files into `dist/assets/media/`. The validation step confirms that folder exists.
