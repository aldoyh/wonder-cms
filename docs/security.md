# Security

WonderCMS is used for editing and build-time rendering. The live public output should be static files from `dist/`, not the editable PHP CMS.

Change the default WonderCMS password and login slug before using the CMS preview outside a trusted local environment.
