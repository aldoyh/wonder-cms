# here.now deployment

Set these repository secrets before enabling live publication:

- `HERENOW_API_KEY`
- `HERENOW_SLUG`

`HERENOW_DRY_RUN=1` keeps the upload and publish scripts in validation mode. Set `HERENOW_DRY_RUN=0` only after adding the production here.now Drive and Sites API calls.
