# Bilingual content

English source pages live in `content/en/`. Arabic source pages live in `content/ar/`.

The page tree and SEO metadata are defined in `content/site.php`. Run `php scripts/hydrate.php` after editing content to regenerate `cms/data/database.js`.
